# How To Build `chill-sharp-react-client`

From [`extra/chill-sharp-react-client`](/c:/source/personal/chill-sharp/chill-sharp/extra/chill-sharp-react-client):

```bash
npm install
npm run build
```

This package depends on the local `chill-sharp-ts-client` source through `file:../chill-sharp-ts-client`.

To publish a `.tgz` archive to the shared npm folder:

```powershell
.\publish-to-shared-folder.ps1
```
